---
const { name, description, profilePicture } = Astro.props;
---

<div class="profile-card">
  <img src={profilePicture} alt={name} class="profile-card__image" />
  <h1>{name}</h1>
  <p>{description}</p>
</div>
